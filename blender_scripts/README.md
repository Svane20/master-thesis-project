# Blender

## Run Blender UI with script execution

1. Run the following command:

````bash
python main.py
````

## Run Blender in the Background

1. Run the following command:

````bash
blender -b -P main.py
````